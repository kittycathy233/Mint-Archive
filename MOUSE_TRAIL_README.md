# 鼠标拖尾效果使用说明

## 功能介绍
为整个游戏添加了简易的鼠标拖尾效果，在游戏顶层显示，不影响游戏正常游玩。

## 实现文件
- `source/MouseTrail.hx` - 鼠标拖尾效果的核心实现类
- `source/BaseState.hx` - 基础状态类，为所有游戏状态提供鼠标拖尾功能
- `source/player/PlayState.hx` - 已更新为继承BaseState，自动获得鼠标拖尾功能

## 控制方式

### 全局快捷键（在任何继承BaseState的状态中都可用）
- **F1** - 切换鼠标拖尾的显示/隐藏
- **F2** - 切换为蓝色拖尾
- **F3** - 切换为红色拖尾
- **F4** - 切换为绿色拖尾
- **F5** - 切换为黄色拖尾
- **F6** - 切换为紫色拖尾
- **F7** - 切换为白色拖尾

## 技术特性
- **无限粒子**: 粒子数量不受限制，根据鼠标移动动态生成
- **更新频率**: 60fps（每0.0167秒更新一次，性能优化）
- **自动内存管理**: 粒子完全透明时自动释放内存，防止内存泄漏
- **混合模式**: 加法混合，产生强烈发光效果
- **粒子形状**: 完美圆形，带有渐变发光效果
- **实时大小渐变**: 粒子从24px平滑缩小到4px，生命周期内持续变化
- **渐变发光连接线**: 连接线具有三层发光效果，粗度从左到右渐变（匹配两端粒子大小）
- **发光效果**: 双层渐变发光（外层光晕+内层亮核）
- **粒子行为**: 粒子生成后固定在原位置，独立进行生命周期过渡
- **生命周期**: 每个粒子有0.65秒生存时间，快速过渡优化性能
- **智能显示**: 只在鼠标移动时生成新粒子，现有粒子自然老化消失

## 性能优化
- **智能生成**: 只在鼠标实际移动时生成新粒子
- **自动清理**: 完全透明的粒子自动从内存中移除
- **动态渲染**: 不可见粒子不参与连接线绘制
- **生命周期管理**: 每个粒子独立管理生命周期，避免全局计算
- **内存安全**: 粒子销毁时正确释放所有资源
- **批量更新**: 使用200fps间隔进行批量状态更新，提供极致流畅体验

## 如何在新状态中使用
1. 让你的状态类继承 `BaseState` 而不是 `FlxState`
2. 调用 `super.create()` 和 `super.update(elapsed)` 
3. 鼠标拖尾效果将自动添加

```haxe
class YourState extends BaseState
{
    override public function create():Void
    {
        super.create(); // 这会自动添加鼠标拖尾
        // 你的初始化代码
    }
    
    override public function update(elapsed:Float):Void
    {
        super.update(elapsed); // 这会处理鼠标拖尾更新
        // 你的更新代码
    }
}
```

## 自定义设置
可以通过 `getMouseTrail()` 方法获取鼠标拖尾对象进行自定义：

```haxe
var trail = getMouseTrail();
if (trail != null) {
    trail.setTrailColor(0xFFFF0000); // 设置为红色
    trail.setEnabled(false); // 禁用拖尾
}